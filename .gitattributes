# Normalise to LF in the repo. Development happens on Windows but CI runs on
# Ubuntu and commits public/data/feed.json — without this, the line-ending
# mismatch shows up as a spurious whole-file diff on every scheduled run.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.webp binary
*.ico binary
