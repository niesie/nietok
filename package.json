{
  "name": "nietok",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A doomscroll feed for geopolitics, economics and history.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:world": "node scripts/build-world.js",
    "ingest": "node --env-file-if-exists=.env scripts/ingest/index.js",
    "validate": "node scripts/validate.js"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.118.0",
    "fast-xml-parser": "^5.11.0",
    "vite": "^8.2.1"
  }
}
