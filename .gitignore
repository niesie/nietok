# Secrets — never commit. This repo is public.
.env
.env.local

# Dependencies
node_modules/

# Build output
dist/

# Ingest scratch
.cache/
.wiki-cache/

# Generated data — regenerated every CI run and carried between runs by the
# Actions cache, not by git. Committing ~2.3 MB every three hours would add
# roughly a gigabyte of history a year for content that is rebuilt anyway.
# Run `npm run ingest` after cloning.
public/data/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
